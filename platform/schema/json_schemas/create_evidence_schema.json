{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Create Motorcycle Evidence",
  "description": "Schema for creating photographic evidence for a motorcycle (HU16)",
  "type": "object",
  "properties": {
    "angle": {
      "type": "string",
      "enum": ["FRONTAL", "LATERAL", "REAR"],
      "description": "Optional photo angle: FRONTAL (Frontal), LATERAL (Side), REAR (Back)"
    },
    "image_url": {
      "type": "string",
      "format": "uri",
      "minLength": 1,
      "pattern": "^https://firebasestorage\\.googleapis\\.com/.*$",
      "description": "Firebase Storage URL for the uploaded image"
    },
    "description": {
      "type": "string",
      "maxLength": 200,
      "description": "Optional description for the evidence photo"
    }
  },
  "required": ["image_url"],
  "additionalProperties": false
}
