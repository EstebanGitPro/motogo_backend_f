{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Update Motorcycle Schema",
  "description": "Schema for motorcycle update (HU44) - all fields optional for partial updates",
  "type": "object",
  "properties": {
    "license_plate": {
      "type": "string",
      "description": "License plate (accepted but immutable, ignored on update)",
      "minLength": 1,
      "maxLength": 10
    },
    "reference_id": {
      "type": "string",
      "description": "Encoded hashid of the motorcycle reference from catalog",
      "minLength": 1
    },
    "year": {
      "type": "integer",
      "description": "Year of manufacture",
      "minimum": 1900,
      "maximum": 2100
    },
    "current_mileage": {
      "type": "integer",
      "description": "Current mileage in kilometers",
      "minimum": 0,
      "maximum": 9999999
    },
    "owner_notes": {
      "type": "string",
      "description": "Optional notes from the owner about the motorcycle",
      "maxLength": 500
    },
    "profile_image_url": {
      "type": "string",
      "description": "HU36 - Optional Firebase Storage URL for motorcycle profile image",
      "maxLength": 500
    }
  },
  "additionalProperties": false
}
