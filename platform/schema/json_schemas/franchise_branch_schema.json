{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Franchise Branch",
  "description": "Schema for adding a branch to a franchise",
  "type": "object",
  "required": ["branch_id"],
  "properties": {
    "branch_id": {
      "type": "string",
      "minLength": 1,
      "description": "Encoded branch ID — CHAR(36) hashid"
    }
  },
  "additionalProperties": false
}
