{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Associate Branch Services",
  "description": "Schema for associating services to a branch",
  "type": "object",
  "required": ["service_ids"],
  "properties": {
    "service_ids": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "minItems": 1,
      "description": "Array of encoded service IDs — CHAR(36) hashids"
    }
  },
  "additionalProperties": false
}
