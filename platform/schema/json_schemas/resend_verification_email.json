{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Resend Verification Email Request",
  "description": "Schema for resending email verification to a registered user",
  "properties": {
    "email": {
      "type": "string",
      "format": "email",
      "description": "Email address of the user",
      "minLength": 5,
      "maxLength": 250,
      "examples": ["usuario@example.com"]
    }
  },
  "required": ["email"],
  "additionalProperties": false
}
